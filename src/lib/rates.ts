/**
 * Median monthly payout per source in USD — the landing page estimator's
 * placeholder economics, quoted anywhere the product projects earnings so
 * every number traces to the same table. An estimate, not a promise.
 */
export const SOURCE_RATES: Record<string, number> = {
  browsing: 9.4,
  purchases: 6.2,
  health: 12.8,
  location: 7.5,
  media: 4.1,
  voice: 8.9,
  messaging: 5.3,
  dashcam: 11.2,
  other: 5.0,
};
